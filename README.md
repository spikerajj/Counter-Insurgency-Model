# Counter-Insurgency-Model 

# cmd to install libraries (python 3.11)

# without internet ->
# pip install --no-index -f . streamlit
# pip install --no-index -f . numpy
# pip install --no-index -f . seaborn
# pip install --no-index -f . matplotlib

# with internet ->
# pip install requirements.txt


# cmd to run
# streamlit run app.py 
